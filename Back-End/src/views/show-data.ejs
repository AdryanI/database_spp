<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div class="container">
      <% if (status == 'admin' || status == 'petugas')  { %>
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">nisn</th>
                    <th scope="col">nama</th>
                    <th scope="col">id spp</th>
                    <th scope="col">nominal</th>
                    <th scope="col">Waktu</th>
                    <th scope="col">PJ</th>
                </tr>
            </thead>
            <tbody>
                <% if (transaksi.length > 0 && (status == 'admin' || status == 'petugas')) { %>
                    <% for (let transaksil of transaksi.sort((a,b)=>a.nama-b.nama)) { %>
                        <tr>
                            <th scope="row"><%= transaksil.nisn %></th>
                            <th scope="row"><%= transaksil.nama %></th>
                            <th scope="row"><%= transaksil.id_spp %></th>
                            <th scope="row"><%= transaksil.nominal %></th>             
                            <th scope="row"><%= transaksil.waktu %></th>
                            <th scope="row"><%= transaksil.penanggung_jawab %></th>
                        </tr>
                    <% } %>
                <% } else if (transaksi.length < 1) { %>
                    <tr>
                        <td colspan="6">Transaksi belum ada.</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    <% } else { %>
        <p>Anda tidak memiliki izin untuk melihat transaksi.</p>
    <% } %>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
    <!-- <script src="script.js"></script> -->
</body>
</html>